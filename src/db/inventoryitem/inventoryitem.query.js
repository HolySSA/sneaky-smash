// export const SQL_QUERIES = {
//     // 아이템 사용하기
//     USE_ITEM: 'UPDATE inventoryitem SET amount = amount - ? WHERE characterId = ? AND itemId = ? AND amount >= ?',

//     // 아이템 버리기
//     DISCARD_ITEM: 'UPDATE inventoryitem SET amount = amount - ? WHERE characterId = ? AND itemId = ? AND amount >= ?',

//     // 아이템의 상태 확인 (아이템 수량이나 장착 여부를 확인?)
//     CHECK_ITEM: 'SELECT amount, isEquipped FROM inventoryitem WHERE characterId = ? AND itemId = ?',

//     // 아이템 장착하기
//     EQUIP_ITEM: 'UPDATE inventoryItem SET isEquipped = TRUE WHERE characterId = ? AND itemId = ?',
// };
