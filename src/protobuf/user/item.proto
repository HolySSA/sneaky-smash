syntax = "proto3";

message ItemInfo {
  int32 itemId = 1; // 아이템 ID
}

message C_UseItem {
  int32 itemId = 1; // 사용할 아이템 ID
}

message S_UseItem {
  int32 playerId = 1; //사용한 유저정보
  int32 hp = 2; // 사용 후 남은 아이템 정보
}
